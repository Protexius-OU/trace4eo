<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter
        xmlns="https://github.com/spotbugs/filter/3.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://github.com/spotbugs/filter/3.0.0 https://raw.githubusercontent.com/spotbugs/spotbugs/master/spotbugs/etc/findbugsfilter.xsd">

    <!--
      EI_EXPOSE_REP / EI_EXPOSE_REP2 fire on every constructor that stores an injected
      reference. In a Spring application, components hold references to other Spring-managed
      beans (Jackson mappers, HTTP clients, other services) by design; defensive copies are
      anti-idiomatic and would defeat the point of DI.
    -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2"/>
    </Match>

    <!-- Test classes and helpers get a lighter pass — checkstyle and errorprone already cover them. -->
    <Match>
        <Class name="~.*(Tests?|TestUtils?|IT)$"/>
    </Match>

</FindBugsFilter>
